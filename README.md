# Estate Apple Style

<div align="center">
  <img src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" alt="Banner" width="100%" />
</div>

## 📋 Обзор

**Estate Apple Style** — это современная платформа недвижимости премиум-класса, разработанная в эстетике Apple. Она использует передовые технологии искусственного интеллекта, предоставляя пользователям интеллектуальные инструменты для покупки, продажи, аренды и управления недвижимостью. Приложение отличается элегантным адаптивным дизайном с плавной анимацией и визуальным стилем «жидкое стекло».

## ✨ Ключевые особенности

-   **Премиальный UI/UX**: Дизайн в стиле Apple с использованием глассморфизма, плавных переходов и высококачественной графики.
-   **Полный спектр услуг по недвижимости**:
    -   **Покупка**: Поиск недвижимости на первичном и вторичном рынках.
    -   **Продажа**: Размещение объектов с описаниями и оценкой, созданными с помощью ИИ.
    -   **Аренда**: Поиск жилья для аренды или сдача собственной недвижимости.
-   **Инструменты на базе ИИ**:
    -   **AI Generator**: Генерация описаний и визуализаций объектов.
    -   **Прогноз инвестиций**: Прогнозирование трендов стоимости недвижимости с помощью ИИ.
    -   **Эко-скан**: Анализ экологических факторов для конкретных локаций.
    -   **Калькулятор ремонта**: Оценка стоимости ремонта на основе параметров объекта.
    -   **Проверка арендаторов**: Скрининг потенциальных арендаторов с использованием ИИ-анализа.
    -   **Умный подбор**: Интеллектуальный подбор объектов под предпочтения пользователя.
-   **Интерактивный дашборд**: Управление объявлениями, просмотр аналитики и отслеживание использования ИИ-инструментов.
-   **Блог и инсайты**: Актуальные новости и советы из мира недвижимости.
-   **Адаптивный дизайн**: Полная оптимизация для десктопов, планшетов и мобильных устройств.

## 🛠 Технологический стек

-   **Frontend Framework**: [React 19](https://react.dev/)
-   **Сборщик**: [Vite](https://vitejs.dev/)
-   **Язык**: [TypeScript](https://www.typescriptlang.org/)
-   **Стилизация**: [Tailwind CSS](https://tailwindcss.com/)
-   **Маршрутизация**: [React Router DOM](https://reactrouter.com/)
-   **Интеграция ИИ**: [Google GenAI SDK](https://ai.google.dev/) (Gemini)
-   **Иконки**: [Lucide React](https://lucide.dev/)
-   **Генерация PDF**: [jsPDF](https://github.com/parallax/jsPDF)

### Backend и База Данных
-   **Runtime**: Node.js
-   **Framework**: Express.js
-   **База данных**: PostgreSQL
-   **ORM/Query Builder**: `pg` (node-postgres)
-   **Аутентификация**: JWT, Bcrypt
-   **Инструменты**: Docker, Docker Compose

## 🏗 Архитектура

Проект следует модульной и компонентной архитектуре:

```
estate-apple-style/
├── src/
│   ├── components/       # Переиспользуемые UI компоненты (Navbar, Footer, Cards и др.)
│   ├── context/          # Управление глобальным состоянием (TrackingContext)
│   ├── pages/            # Страницы приложения (Home, Dashboard, AI Tools)
│   ├── services/         # Бизнес-логика и интеграции API (AI, Data)
│   ├── App.tsx           # Основной компонент приложения и настройка маршрутизации
│   └── main.tsx          # Точка входа
├── backend/              # Серверная часть (Node.js + Express)
│   ├── middleware/       # Middleware (авторизация и др.)
│   ├── server.js         # Точка входа API
│   └── seed.js           # Скрипт инициализации БД
├── public/               # Статические ресурсы
└── ...конфигурационные файлы
```

### Ключевые директории

-   **`components/`**: Содержит атомарные и составные элементы UI, такие как `GlassCard`, `Navbar`, `ChatWidget`, и специфические компоненты разделов.
-   **`pages/`**: Представляет основные экраны приложения, напрямую сопоставленные с маршрутами в `App.tsx`.
-   **`services/`**: Обрабатывает получение внешних данных и взаимодействия с ИИ-сервисами (`aiService.ts`, `api.ts`).
-   **`context/`**: Управляет глобальным состоянием приложения, например, отслеживанием действий пользователя и настройками.
-   **`backend/`**: Содержит логику API, подключение к базе данных PostgreSQL и скрипты миграции/сидинга. База данных имеет расширенную структуру (более 30 колонок) для хранения детальной информации об объектах: технические характеристики, данные о застройщике, сроки сдачи, варианты отделки и ценообразование.

## � API и База Данных

### API Эндпоинты

Бэкенд предоставляет REST API для взаимодействия с данными:

| Метод | Эндпоинт | Описание |
| :--- | :--- | :--- |
| `GET` | `/api/health` | Проверка статуса сервера и соединения с БД |
| `POST` | `/api/auth/login` | Аутентификация администратора (возвращает JWT) |
| `GET` | `/api/estates` | Получение списка всех объектов недвижимости |
| `POST` | `/api/estates/upload` | Загрузка CSV файла с объектами (требует Auth) |

### Схема Базы Данных (PostgreSQL)

Таблица `estates` хранит исчерпывающую информацию об объектах недвижимости (более 30 колонок):

#### 1. Основные данные (Карточки)
*   `id`, `title` (Название ЖК)
*   `price` (Отображаемая цена), `price_val` (Числовая цена)
*   `image` (Обложка), `location` (Адрес)
*   `specs` (Характеристики), `tags` (Теги)

#### 2. Детальные характеристики
*   **О проекте**: `jk_name`, `developer`, `description`, `source_url`
*   **Строительство**: `deadline` (Срок сдачи), `deadline_keys` (Ключи), `house_type` (Тип дома)
*   **Параметры**: `floors` (Этажность), `ceiling_height` (Потолки), `finishing` (Отделка), `parking`
*   **Условия**: `contract_type` (Договор), `escrow_bank`, `payment_options`, `registration`

#### 3. Технические данные
*   **Структура**: `buildings_for_sale`, `objects_for_sale`
*   **JSON Данные**: `price_details_json`, `grid_price_info_json`
*   **Медиа**: `image_urls` (Галерея), `local_folder`
*   **Фильтры**: `type`, `area`, `district`, `city`

## �🚀 Начало работы

Вы можете запустить проект двумя способами: через Docker (рекомендуется) или вручную.

### Вариант 1: Docker (Рекомендуется)

1.  Убедитесь, что у вас установлены **Docker** и **Docker Compose**.
2.  Запустите проект командой:
    ```bash
    docker-compose up --build
    ```
    Это автоматически поднимет базу данных PostgreSQL, бэкенд и фронтенд.

### Вариант 2: Ручная установка

### Предварительные требования

-   **Node.js**: v18+
-   **PostgreSQL**: Локально установленная и запущенная база данных.

### Установка

1.  **Клонируйте репозиторий** (если применимо) или перейдите в директорию проекта.

2.  **Установите зависимости**:
    ```bash
    npm install
    ```

3.  **Настройка окружения**:
    *   Создайте файл `.env.local` в корневой директории для фронтенда:
        ```env
        GEMINI_API_KEY=ваш_api_ключ_здесь
        ```
    *   Убедитесь, что база данных PostgreSQL запущена и доступна (настройте `.env` в папке `backend/` при необходимости).

4.  **Запуск Бэкенда**:
    Откройте новый терминал:
    ```bash
    cd backend
    npm install
    npm run seed # (Опционально) Заполнить БД тестовыми данными
    npm start
    ```

5.  **Запуск Фронтенда**:
    В корневой директории:
    ```bash
    npm run dev
    ```
    Приложение будет доступно по адресу `http://localhost:3000`.

## 📦 Сборка для продакшена

Чтобы собрать приложение для продакшена:

```bash
npm run build
```

Для локального предпросмотра собранной версии:

```bash
npm run preview
```

## 🤝 Вклад в проект

Мы приветствуем ваш вклад! Пожалуйста, не стесняйтесь отправлять Pull Request.

## 📄 Лицензия

[MIT License](LICENSE)
